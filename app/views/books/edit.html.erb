<h1>Editing Book</h1>

<%= form_with model: @book, url: book_path(@book.id), method: :patch do |f| %>

<label for="book_title">Title</laber>
<div class="field">
  <%= f.text_field :title %>
</div>
<label for="book_body">Body</laber>
<div class="field">
  <%= f.text_area :body %>
</div>
<div class="actions">
  <%= f.submit 'Update Book' %>
</div>
<% end %>

<td><%= link_to "Show", book_path(@book.id) %></td>
|
<td><%= link_to "Back", books_path %></td>